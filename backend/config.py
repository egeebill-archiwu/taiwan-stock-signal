"""
台股布林通道交易訊號系統 - 全域設定檔

包含布林通道參數、交易策略參數、資料庫路徑及 Telegram 通知設定。
"""

import os
from pathlib import Path

# ============================================================
# 專案路徑
# ============================================================
BASE_DIR = Path(__file__).resolve().parent
PROJECT_DIR = BASE_DIR.parent
DATA_DIR = PROJECT_DIR / "data"
DATA_DIR.mkdir(parents=True, exist_ok=True)

# ============================================================
# 資料庫設定
# ============================================================
DATABASE_PATH = DATA_DIR / "stock_signals.db"
DATABASE_URL = f"sqlite:///{DATABASE_PATH}"

# ============================================================
# 布林通道參數
# ============================================================
BB_PERIOD: int = 20          # 布林通道計算週期（天）
BB_STD: float = 2.0          # 布林通道標準差倍數

# ============================================================
# 均線參數
# ============================================================
MA_PERIOD: int = 10           # 移動平均線週期（天）

# ============================================================
# 交易訊號參數
# ============================================================
CONSECUTIVE_K_THRESHOLD: int = 3   # 最少連續 K 棒數量
TREND_LOOKBACK: int = 5            # 判斷中軌斜率的回顧天數

# ============================================================
# 布林帶寬臨界值（用於趨勢判斷）
# ============================================================
BANDWIDTH_EXPANDING_THRESHOLD: float = 0.0   # 帶寬變化 > 此值視為擴張中

# ============================================================
# 訊號觸發：收盤價與上/下軌距離百分比門檻
# ============================================================
NEAR_BAND_PCT: float = 0.05   # 收盤價距上/下軌在此百分比內視為「靠近」（5%）

# ============================================================
# 回測預設參數
# ============================================================
DEFAULT_INITIAL_CAPITAL: float = 1_000_000.0   # 預設初始資金（新台幣）
SHARES_PER_LOT: int = 1000                     # 台股 1 張 = 1000 股

# ============================================================
# Telegram 通知設定（請自行填入）
# ============================================================
TELEGRAM_BOT_TOKEN: str = os.getenv("TELEGRAM_BOT_TOKEN", "YOUR_TELEGRAM_BOT_TOKEN")
TELEGRAM_CHAT_ID: str = os.getenv("TELEGRAM_CHAT_ID", "YOUR_TELEGRAM_CHAT_ID")

# ============================================================
# yfinance 相關
# ============================================================
DEFAULT_FETCH_PERIOD: str = "1y"   # 預設抓取資料期間
TWSE_SUFFIX: str = ".TW"          # 上市股票後綴
TPEX_SUFFIX: str = ".TWO"         # 上櫃股票後綴
